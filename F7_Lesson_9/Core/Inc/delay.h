/*
 * Include guards are used to prevent a file, actually the contents of a file,
 * from being included more than once.
 *
 * #ifndef __FILE_H__
 * #define __FILE_H__
 * #endif // __FILE_H__
 */

#ifndef __DELAY_H__
#define __DELAY_H__

void delay(int iter);

#endif // __DELAY_H__
